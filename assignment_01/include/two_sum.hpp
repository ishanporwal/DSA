#pragma once

#include <vector>

/**
 * Finds two indices whose values sum to the target.
 *
 * @param nums Vector of integers to search.
 * @param target Desired sum.
 * @return The two matching indices, or an empty vector if no pair exists.
 */

std::vector<int> twoSum(const std::vector<int>& nums, int target);
